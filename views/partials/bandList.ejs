<datalist id="knownBands">
    <% 
    var dataObj = knownBandData;
    %>
    <% for (let singleItem of dataObj) { %>
        <option value='<%= singleItem %>'>
            <% } %>
        </datalist>